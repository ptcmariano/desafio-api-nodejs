
class Middleware {
    constructor(req, res, next)
    {
        
        // if (!!req.body.user.password) {
            // todo no show pass
        // }

        return this;
    }
}

module.exports = Middleware;